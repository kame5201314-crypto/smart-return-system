// Test stub for `server-only` / `client-only` marker packages, which throw when
// imported outside their intended runtime. Aliased in vitest.config.ts so that
// server-only modules can be unit-tested under vitest.
export {};
